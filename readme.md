## Golang Easy Examples

**Estos son ejemplos sencillos sobre el lenguaje golang, agregare mas mientras aprenda.**

`package grettings

import "fmt"

func Hola(nombre string) string {
message := fmt.Sprintf("Hola, %v. Welcome", nombre)
return message
}
`
